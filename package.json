{
  "name": "ai-dispatch-portfolio",
  "version": "1.0.0",
  "description": "Production AI voice and text dispatcher - portfolio showcase",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src --ext .ts,.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "zod": "^4.0.0"
  }
}